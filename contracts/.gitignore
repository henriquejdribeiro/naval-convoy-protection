# Foundry build artifacts
out/
cache/
broadcast/

# Dependencies (installed via `forge install`).
# starkex-contracts is a tracked submodule — gitignore per-subdir
# instead of blanket-ignoring lib/, so the submodule entry sticks.
lib/openzeppelin-contracts/
lib/forge-std/

# Coverage / debug
*.dbg.json
report/
